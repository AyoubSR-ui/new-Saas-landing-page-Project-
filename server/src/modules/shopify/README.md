# shopify module

Isolated Shopify integration layer (OAuth, API client, product sync, webhooks, checkout).
Populated starting Phase 1 (auth/, client/, webhooks/) and Phase 2 (products/), Phase 9 (checkout/).
No Shopify API calls should be made from outside this module.
