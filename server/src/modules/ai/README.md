# ai module

ai.service.ts, promptBuilder.ts, pageGenerator.ts, pageValidator.ts — product context to
structured page JSON pipeline (AI is an engine, never a source of executable code).
Populated in Phase 6.
