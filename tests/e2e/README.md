# End-to-end tests

Populated once both the visual editor (Phase 5) and publishing (Phase 8) exist, covering
the full flow: install/open app -> select product -> generate page -> edit -> save ->
publish -> open published page -> click CTA.
