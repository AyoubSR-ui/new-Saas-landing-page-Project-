# Migrations

Prisma migrations live in `server/prisma/migrations` (generated by `prisma migrate dev`).
This top-level folder is reserved per the architecture doc for any migration tooling that
needs to operate outside the server package's own Prisma workflow.
